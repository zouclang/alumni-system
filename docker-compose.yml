version: '3.8'

services:
  alumni-app:
    build: .
    container_name: alumni-system
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
