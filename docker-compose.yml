version: '3.8'

services:
  alumni-app:
    build: .
    container_name: alumni-system
    ports:
      - "8085:8085"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8085
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
    restart: always
